{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "preDeployCommand": "php artisan migrate --force && php artisan storage:link",
        "startCommand": "php artisan route:clear && php artisan config:clear && php artisan view:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
    }
}